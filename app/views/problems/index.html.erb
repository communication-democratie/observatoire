<% content_for :title, 'Publicités signalées' %>

<% content_for :head do %>
  <link rel="canonical" href="<%= problems_path %>" />
  <% if @facets.has_params? %>
    <meta name="robots" content="noindex">
  <% end %>
<% end %>

<% content_for :hero do %>
  <div class="container">
    <div class="row">
      <h1>Publicités signalées</h1>
    </div>
  </div>
<% end %>

<div class="row">
  <div class="col-md-3 search-filters accordion">
    <%= render 'faceted_search/facets', facets: @facets %>
  </div>
  <div class="col-md-9">
    <div class="mb-5">
      <%= render 'problems/list', problems: @problems %>
    </div>
    <%= paginate @problems, theme: 'bootstrap-5' %>
  </div>
</div>
