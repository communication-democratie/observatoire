<% content_for :title, 'Nos analyses' %>

<% content_for :hero do %>
  <div class="container">
    <div class="row">
      <h1>Nos analyses</h1>
    </div>
  </div>
<% end %>

<% @categories.each do |category| %>
  <section class="mt-5">
    <h2><%= category %></h2>
    <%= render 'list',
                problems: category.problems
                                  .important
                                  .ordered
                                  .limit(3) %>
  </section>
<% end %>