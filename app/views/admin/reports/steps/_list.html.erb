<table class="table">
  <tbody>
    <% steps.each do |step| %>
      <tr>
        <td width="50%">
          <%= link_to step.title, admin_reports_step_path(step) %>
          <% if step.ignore_in_front? %>
            <span class="badge bg-secondary"><%= t('admin.ignored_in_front') %></span>
          <% end %>
        </td>
        <td><%= step.description %></td>
        <td><%= step.position %></td>
      </tr>
    <% end %>
  </tbody>
</table>